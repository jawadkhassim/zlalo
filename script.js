(() => {
  const translations = {
    en: {
      meta: {
        title: 'zlalo | Premium AI Smartphones',
        description: 'zlalo creates premium smartphones and intelligent AI technology for the next generation of mobile experiences.'
      },
      nav: {
        home: 'Home',
        products: 'Products',
        ai: 'AI Technology',
        about: 'About',
        contact: 'Contact',
        menuToggle: 'Toggle navigation'
      },
      hero: {
        eyebrow: 'Premium AI smartphone innovation',
        heading: 'Crafting beautiful devices with intelligence that feels effortless.',
        text: 'zlalo blends ultra-premium hardware, adaptive AI, and secure cloud experiences into smartphone technology that helps you create, connect, and move faster.',
        ctaPrimary: 'Explore products',
        ctaSecondary: 'Book a consultation',
        highlights: ['Neural AI assistant', 'Pro-grade camera system', 'Private on-device intelligence'],
        visualBadge: 'Live',
        visualLabel: 'Adaptive intelligence',
        visualHeading: 'Always ready to assist.'
      },
      stats: [
        { label: 'AI assistance' },
        { label: 'Reliability' },
        { label: 'Faster workflow' }
      ],
      products: {
        eyebrow: 'Smartphone products',
        heading: 'Premium technology built for power, clarity, and everyday elegance.',
        cards: [
          {
            badge: 'Flagship',
            description: 'Ultra-slim design with a cinematic display, advanced photography, and intelligent performance tuning.',
            list: ['12GB AI memory', '120Hz AMOLED display', 'All-day battery optimization']
          },
          {
            badge: 'Creator',
            description: 'Built for creators who need seamless capture, editing, and content delivery on the go.',
            list: ['Studio-quality camera', 'Instant voice-to-content tools', 'Lightweight premium frame']
          },
          {
            badge: 'Business',
            description: 'Security-first smartphones designed for modern teams, executive mobility, and collaborative workflows.',
            list: ['Secure device management', 'Enterprise AI assistant', 'Reliable multi-device sync']
          }
        ]
      },
      ai: {
        eyebrow: 'AI technology',
        heading: 'Intelligence that adapts to the way you work and live.',
        text: 'Our AI systems learn from context, protect your privacy, and deliver proactive suggestions that feel natural rather than intrusive.',
        features: [
          { title: 'Context-aware assistance', text: 'Personalized support that understands your routines and priorities.' },
          { title: 'On-device security', text: 'Private processing and secure data handling built into every experience.' }
        ],
        panelValue: 'Adaptive'
      },
      about: {
        eyebrow: 'About us',
        heading: 'Where elegant design meets ambitious technology.',
        text: 'zlalo was created to reimagine the smartphone experience for the AI era. We focus on premium craftsmanship, thoughtful software, and intelligent systems that help people feel more connected and more in control.',
        listHeading: 'Why clients choose zlalo',
        list: ['Luxury-grade product design', 'Secure and adaptive AI experiences', 'Reliable support for modern everyday use']
      },
      contact: {
        eyebrow: 'Contact us',
        heading: 'Let’s shape the next chapter of mobile intelligence together.',
        text: 'Share your vision and our team will help you explore the right zlalo solution.',
        form: {
          name: 'Name',
          email: 'Email',
          message: 'Message',
          namePlaceholder: 'Your name',
          emailPlaceholder: 'you@example.com',
          messagePlaceholder: 'Tell us more about your idea',
          submit: 'Send message',
          validation: {
            name: 'Please enter your name.',
            email: 'Please enter a valid email address.',
            message: 'Please share a bit more detail so we can help.',
            success: 'Thanks! We will get back to you soon.'
          }
        }
      },
      footer: {
        prefix: '©',
        suffix: 'Designed for the future.'
      }
    },
    fr: {
      meta: {
        title: 'zlalo | Smartphones IA premium',
        description: 'zlalo crée des smartphones premium et une technologie d’IA intelligente pour la prochaine génération d’expériences mobiles.'
      },
      nav: {
        home: 'Accueil',
        products: 'Produits',
        ai: 'Technologie IA',
        about: 'À propos',
        contact: 'Contact',
        menuToggle: 'Basculer la navigation'
      },
      hero: {
        eyebrow: 'Innovation premium en smartphones IA',
        heading: 'Créer des appareils élégants avec une intelligence qui semble naturelle.',
        text: 'zlalo combine du matériel ultra-premium, une IA adaptative et des expériences cloud sécurisées dans une technologie de smartphone qui vous aide à créer, à rester connecté et à avancer plus vite.',
        ctaPrimary: 'Découvrir les produits',
        ctaSecondary: 'Prendre rendez-vous',
        highlights: ['Assistant IA neuronal', 'Système photo de qualité professionnelle', 'Intelligence privée sur l’appareil'],
        visualBadge: 'En direct',
        visualLabel: 'Intelligence adaptative',
        visualHeading: 'Toujours prêt à aider.'
      },
      stats: [
        { label: 'Assistance IA' },
        { label: 'Fiabilité' },
        { label: 'Flux de travail plus rapide' }
      ],
      products: {
        eyebrow: 'Produits smartphones',
        heading: 'Une technologie premium pensée pour la puissance, la clarté et l’élégance du quotidien.',
        cards: [
          {
            badge: 'Flagship',
            description: 'Design ultra-fin avec écran cinématographique, photographie avancée et réglages de performance intelligents.',
            list: ['Mémoire IA de 12 Go', 'Écran AMOLED 120 Hz', 'Optimisation de batterie toute la journée']
          },
          {
            badge: 'Créateurs',
            description: 'Conçu pour les créateurs qui ont besoin d’une capture, d’un montage et d’une diffusion fluides en déplacement.',
            list: ['Appareil photo de qualité studio', 'Outils instantanés de voix à contenu', 'Cadre premium léger']
          },
          {
            badge: 'Entreprise',
            description: 'Smartphones axés sur la sécurité conçus pour les équipes modernes, la mobilité executive et les workflows collaboratifs.',
            list: ['Gestion sécurisée des appareils', 'Assistant IA d’entreprise', 'Synchronisation multi-appareils fiable']
          }
        ]
      },
      ai: {
        eyebrow: 'Technologie IA',
        heading: 'Une intelligence qui s’adapte à votre façon de travailler et de vivre.',
        text: 'Nos systèmes d’IA apprennent du contexte, protègent votre vie privée et proposent des suggestions proactives qui semblent naturelles plutôt qu’intrusives.',
        features: [
          { title: 'Assistance contextuelle', text: 'Un accompagnement personnalisé qui comprend vos habitudes et vos priorités.' },
          { title: 'Sécurité sur l’appareil', text: 'Traitement privé et gestion sécurisée des données intégrés à chaque expérience.' }
        ],
        panelValue: 'Adaptatif'
      },
      about: {
        eyebrow: 'À propos',
        heading: 'Où le design élégant rencontre une technologie ambitieuse.',
        text: 'zlalo a été créé pour réinventer l’expérience du smartphone à l’ère de l’IA. Nous mettons l’accent sur un savoir-faire premium, un logiciel réfléchi et des systèmes intelligents qui aident les gens à se sentir plus connectés et plus maîtres de leur expérience.',
        listHeading: 'Pourquoi les clients choisissent zlalo',
        list: ['Design produit de qualité premium', 'Expériences IA sécurisées et adaptatives', 'Support fiable pour l’usage moderne du quotidien']
      },
      contact: {
        eyebrow: 'Contactez-nous',
        heading: 'Construisons ensemble le prochain chapitre de l’intelligence mobile.',
        text: 'Partagez votre vision et notre équipe vous aidera à explorer la solution zlalo adaptée à vos besoins.',
        form: {
          name: 'Nom',
          email: 'Courriel',
          message: 'Message',
          namePlaceholder: 'Votre nom',
          emailPlaceholder: 'vous@example.com',
          messagePlaceholder: 'Dites-nous en plus sur votre idée',
          submit: 'Envoyer le message',
          validation: {
            name: 'Veuillez entrer votre nom.',
            email: 'Veuillez entrer une adresse courriel valide.',
            message: 'Veuillez partager un peu plus de détails pour que nous puissions vous aider.',
            success: 'Merci ! Nous reviendrons vers vous bientôt.'
          }
        }
      },
      footer: {
        prefix: '©',
        suffix: 'Conçu pour l’avenir.'
      }
    },
  };

  const storageKey = 'zlalo-language';
  const header = document.querySelector('.site-header');
  const menuToggle = document.querySelector('.menu-toggle');
  const nav = document.querySelector('.nav');
  const navLinks = Array.from(document.querySelectorAll('.nav a'));
  const sections = navLinks
    .map((link) => document.querySelector(link.getAttribute('href')))
    .filter(Boolean);
  const form = document.getElementById('contact-form');
  const formStatus = document.querySelector('.form-status');
  const year = document.getElementById('year');
  const heroVisual = document.querySelector('.hero-visual');
  const languageButtons = Array.from(document.querySelectorAll('.lang-btn'));
  let currentLanguage = 'en';

  if (year) {
    year.textContent = new Date().getFullYear();
  }

  const getValue = (source, path) => {
    if (typeof path !== 'string' || path.length === 0) {
      return undefined;
    }

    return path.split('.').reduce((accumulator, key) => {
      if (!accumulator || typeof accumulator !== 'object') {
        return undefined;
      }
      return accumulator[key];
    }, source);
  };

  const t = (key) => {
    if (typeof key !== 'string' || key.length === 0) {
      return '';
    }

    const value = getValue(translations[currentLanguage], key);
    if (value !== undefined) {
      return value;
    }
    return getValue(translations.en, key) ?? '';
  };

  const updateTextContent = (element, value) => {
    if (element instanceof HTMLInputElement || element instanceof HTMLTextAreaElement) {
      element.value = value;
      return;
    }

    element.textContent = value;
  };

  const setLanguage = (lang) => {
    const normalizedLang = translations[lang] && Object.keys(translations[lang]).length ? lang : 'en';
    currentLanguage = normalizedLang;

    document.documentElement.lang = normalizedLang;
    document.documentElement.dir = normalizedLang === 'ar' ? 'rtl' : 'ltr';
    document.title = t('meta.title');
    const description = document.querySelector('meta[name="description"]');
    if (description) {
      description.setAttribute('content', t('meta.description'));
    }

    document.querySelectorAll('[data-i18n]').forEach((element) => {
      const value = t(element.getAttribute('data-i18n'));
      if (value !== '') {
        updateTextContent(element, value);
      }
    });

    document.querySelectorAll('[data-i18n-placeholder]').forEach((element) => {
      const value = t(element.getAttribute('data-i18n-placeholder'));
      if (value !== '') {
        element.setAttribute('placeholder', value);
      }
    });

    document.querySelectorAll('[data-i18n-aria]').forEach((element) => {
      const value = t(element.getAttribute('data-i18n-aria'));
      if (value !== '') {
        element.setAttribute('aria-label', value);
      }
    });

    languageButtons.forEach((button) => {
      const isActive = button.dataset.lang === normalizedLang;
      button.classList.toggle('active', isActive);
      button.setAttribute('aria-pressed', String(isActive));
    });

    localStorage.setItem(storageKey, normalizedLang);
  };

  const setActiveLink = () => {
    const offset = window.scrollY + 140;
    let currentId = 'home';

    sections.forEach((section) => {
      if (section.offsetTop <= offset) {
        currentId = section.id;
      }
    });

    navLinks.forEach((link) => {
      link.classList.toggle('active', link.getAttribute('href') === `#${currentId}`);
    });
  };

  const closeMobileMenu = () => {
    if (!nav || !menuToggle) return;
    nav.classList.remove('nav-open');
    menuToggle.setAttribute('aria-expanded', 'false');
  };

  const openMobileMenu = () => {
    if (!nav || !menuToggle) return;
    nav.classList.add('nav-open');
    menuToggle.setAttribute('aria-expanded', 'true');
  };

  const showFormMessage = (message, isError = false) => {
    if (!formStatus) return;
    formStatus.textContent = message;
    formStatus.className = 'form-status';
    formStatus.classList.add(isError ? 'error' : 'success');
  };

  const initializeRevealAnimations = () => {
    const revealTargets = document.querySelectorAll('.reveal');

    if (!revealTargets.length) return;

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.14 }
    );

    revealTargets.forEach((element) => observer.observe(element));
  };

  navLinks.forEach((link) => {
    link.addEventListener('click', (event) => {
      const targetId = link.getAttribute('href');
      const target = targetId ? document.querySelector(targetId) : null;

      if (!target || targetId === '#') return;

      event.preventDefault();
      const offsetTop = target.getBoundingClientRect().top + window.scrollY - 82;
      window.scrollTo({ top: offsetTop, behavior: 'smooth' });

      if (window.innerWidth <= 760) {
        closeMobileMenu();
      }
    });
  });

  if (menuToggle && nav) {
    menuToggle.addEventListener('click', () => {
      const isOpen = nav.classList.contains('nav-open');
      isOpen ? closeMobileMenu() : openMobileMenu();
    });
  }

  document.addEventListener('click', (event) => {
    if (window.innerWidth > 760) return;
    if (nav && nav.classList.contains('nav-open') && !nav.contains(event.target) && !menuToggle?.contains(event.target)) {
      closeMobileMenu();
    }
  });

  languageButtons.forEach((button) => {
    button.addEventListener('click', () => {
      setLanguage(button.dataset.lang || 'en');
    });
  });

  if (heroVisual) {
    heroVisual.addEventListener('mousemove', (event) => {
      const rect = heroVisual.getBoundingClientRect();
      const x = (event.clientX - rect.left) / rect.width - 0.5;
      const y = (event.clientY - rect.top) / rect.height - 0.5;
      heroVisual.style.transform = `perspective(900px) rotateY(${x * 8}deg) rotateX(${y * -8}deg)`;
    });

    heroVisual.addEventListener('mouseleave', () => {
      heroVisual.style.transform = 'perspective(900px) rotateY(0deg) rotateX(0deg)';
    });
  }

  window.addEventListener('scroll', () => {
    if (header) {
      header.classList.toggle('scrolled', window.scrollY > 20);
    }
    setActiveLink();
  });

  window.addEventListener('resize', () => {
    if (window.innerWidth > 760) {
      closeMobileMenu();
    }
  });

  if (form) {
    form.addEventListener('submit', (event) => {
      event.preventDefault();

      const formData = new FormData(form);
      const name = String(formData.get('name') || '').trim();
      const email = String(formData.get('email') || '').trim();
      const message = String(formData.get('message') || '').trim();
      const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      const fields = form.querySelectorAll('input, textarea');

      fields.forEach((field) => field.classList.remove('is-invalid'));

      if (name.length < 2) {
        form.querySelector('input[name="name"]')?.classList.add('is-invalid');
        showFormMessage(t('contact.form.validation.name'), true);
        return;
      }

      if (!emailPattern.test(email)) {
        form.querySelector('input[name="email"]')?.classList.add('is-invalid');
        showFormMessage(t('contact.form.validation.email'), true);
        return;
      }

      if (message.length < 10) {
        form.querySelector('textarea[name="message"]')?.classList.add('is-invalid');
        showFormMessage(t('contact.form.validation.message'), true);
        return;
      }

      showFormMessage(t('contact.form.validation.success'), false);
      form.reset();
    });
  }

  const savedLanguage = localStorage.getItem(storageKey);
  setLanguage(savedLanguage && translations[savedLanguage] ? savedLanguage : 'en');
  setActiveLink();
  initializeRevealAnimations();
})();
